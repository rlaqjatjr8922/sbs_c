#include <stdio.h>
/*
    문자열단우의 입출력
    1. puts(문자열): 문자열 단위로 출력
    2.gets(포인터) :문자열 단위로 입력

*/
int main() {

    char ch[30];
    gets(ch);
    puts(ch);
    //출력후 자동으로 줄바꿈
    printf("over");
return 0;

}